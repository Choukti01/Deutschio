[build]
builder = "RAILPACK"

[deploy]
startCommand = "pnpm --filter deutschio-api start"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
