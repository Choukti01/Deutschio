[build]
builder = "RAILPACK"

[deploy]
startCommand = "npm run start --workspace=@deutschio/api"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
